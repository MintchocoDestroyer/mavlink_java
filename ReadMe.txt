튜닝 브랜치에 업로드 되는 마브링크 코드 입니다

마스터 브랜치에는 원본 코드가 그대로 있으니 개조가 가능합니다

빌드소스(buildSrc)와 마브링크프로토콜(mavlink-protocol)은 메인 소스(src)와 동일한 코드가 있으며

차이점은 빌드소스에서 mavgen-java로 어노테이션프로세스를 사용해 자바에서 사용하게 변환해주는 방식입니다

마브링크프로토콜은 마브링크 원본 코드에서 이 코드에 필요한 클래스만 옮겨놓은 것이므로 주석을 달지 않습니다
(빌드소스도 마찬가지입니다)